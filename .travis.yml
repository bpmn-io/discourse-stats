jobs:
  include:
    - stage: test
      script:
      - echo "no tests"
    - stage: "testtest"
      language: node_js
      node_js:
        - node
      script:
      - if [[ "$(date -u +%d)" = "28" ]]; then npm start; fi
    - stage: "nightly"
      language: node_js
      node_js:
        - node
      script:
      - if [[ "$(date -u +%d)" = "28" ]]; then npm start; fi

stages:
  - test
  - testtest
  - name: "nightly"
    if: type IN (cron)